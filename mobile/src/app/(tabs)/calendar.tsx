import { PhaseStub } from '~/components/phase-stub';

export default function Calendar() {
  return (
    <PhaseStub icon="calendar-outline" title="Calendar" phase="Phase 2">
      Every commitment, upcoming and past — friendlies, tournaments, ringer games and pitch
      bookings, with availability buttons on each card.
    </PhaseStub>
  );
}

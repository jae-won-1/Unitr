import { PhaseStub } from '~/components/phase-stub';

export default function MyTeam() {
  return (
    <PhaseStub icon="people-outline" title="My Team" phase="Phase 2 · 4">
      Squad, stats and fixtures, plus the captain&apos;s control panel — tactics, settle
      payments, team settings and match management.
    </PhaseStub>
  );
}

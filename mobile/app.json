{
  "expo": {
    "name": "Uniter",
    "slug": "uniter",
    "version": "0.1.0",
    "orientation": "portrait",
    "icon": "./assets/images/icon.png",
    "scheme": "uniter",
    "//userInterfaceStyle": "LIGHT, not automatic. The web app is light-only -- it has no dark theme to mirror -- so following the device's dark mode made the mobile app diverge from it on exactly the phones most likely to be used. Locking this also stops native chrome (status bar, sheets, keyboard) going dark around light screens. Revisit only if the web app gains a dark theme, at which point src/theme.ts already has the palette.",
    "userInterfaceStyle": "light",
    "ios": {
      "bundleIdentifier": "com.uniter.app",
      "supportsTablet": false
    },
    "android": {
      "package": "com.uniter.app",
      "adaptiveIcon": {
        "backgroundColor": "#00E676",
        "foregroundImage": "./assets/images/android-icon-foreground.png",
        "backgroundImage": "./assets/images/android-icon-background.png",
        "monochromeImage": "./assets/images/android-icon-monochrome.png"
      },
      "predictiveBackGestureEnabled": false
    },
    "web": {
      "output": "static",
      "favicon": "./assets/images/favicon.png"
    },
    "plugins": [
      "expo-router",
      [
        "expo-splash-screen",
        {
          "backgroundColor": "#008000",
          "image": "./assets/images/splash-icon.png",
          "imageWidth": 76
        }
      ],
      "expo-font"
    ],
    "experiments": {
      "typedRoutes": true,
      "reactCompiler": true,
      "//tsconfigPaths": "OFF deliberately. Expo otherwise feeds tsconfig paths to Metro as well, which makes the two inseparable -- and this app needs one mapping that must apply to TypeScript ONLY: react -> node_modules/@types/react, which redirects the shared repo-root contexts away from the web app's React 18 typings. Metro following that mapping resolves react to a types-only package with no runtime and the bundle dies. Module aliases therefore live in metro.config.js and are mirrored by hand in tsconfig.json.",
      "tsconfigPaths": false
    }
  }
}
